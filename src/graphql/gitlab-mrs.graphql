query GitlabMRs($projectPath: ID!, $iids: [String!]) {
  project(fullPath: $projectPath) {
    id
    name
    path
    fullPath
    mergeRequests(iids: $iids) {
      nodes {
        ...MergeRequestFields
      }
      pageInfo {
        hasNextPage
      }
    }
  }
}
